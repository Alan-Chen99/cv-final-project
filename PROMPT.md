Complete the following user request:

<request added="original (start of loop1)" from="user" commit="77c8a4e" time="2026-05-09 00:17 EDT">

Task: Add `SwinIR finetuning` (train for 2hr), `SwinIR zero shot`. Do some tuning first esp if prior experiments on this just assumed some hyperparameters.

Not experiment: do in `./src` directly, update plots and data.

MUST: You have up to **one** gpu node to use at any given time. I have a limit of 2 "normal" and 4 "preemptable"; Check squeue to decide to use normal or preemptable. This is not enforced externally: you must keep track yourself.

</request>
